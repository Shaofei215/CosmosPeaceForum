version: '3.8'

services:
  agent-scheduler:
    build: .
    container_name: herta-tree-agents
    restart: unless-stopped
    ports:
      - "8001:8001"
    env_file:
      - ./.env
    volumes:
      # 持久化管理数据库
      - ./data:/app/agents/management/data
