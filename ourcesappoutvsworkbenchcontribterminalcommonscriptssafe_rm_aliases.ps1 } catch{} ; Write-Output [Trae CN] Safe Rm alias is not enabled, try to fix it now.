warning: in the working copy of 'social_platform/app/models.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/social_platform/app/models.py b/social_platform/app/models.py[m
[1mindex ebfe972..43a0067 100644[m
[1m--- a/social_platform/app/models.py[m
[1m+++ b/social_platform/app/models.py[m
[36m@@ -1,14 +1,22 @@[m
 """[m
 数据库模型定义[m
[31m-定义所有数据表结构：User, Post, Comment, Like, Follow[m
[32m+[m[32m定义所有数据表结构：User, Post, Comment, Reply, Like, Follow[m
 """[m
[31m-from sqlalchemy import Column, Integer, String, Text, DateTime, ForeignKey[m
[32m+[m[32mfrom sqlalchemy import Column, Integer, String, Text, DateTime, ForeignKey, Enum[m
 from sqlalchemy.orm import relationship[m
 from datetime import datetime[m
[32m+[m[32mimport enum[m
 [m
 from app.database import Base[m
 [m
 [m
[32m+[m[32mclass LikeTargetType(str, enum.Enum):[m
[32m+[m[32m    """点赞目标类型"""[m
[32m+[m[32m    POST = "post"[m
[32m+[m[32m    COMMENT = "comment"[m
[32m+[m[32m    REPLY = "reply"[m
[32m+[m
[32m+[m
 class User(Base):[m
     """[m
     用户模型[m
[36m@@ -22,6 +30,7 @@[m [mclass User(Base):[m
 [m
     posts = relationship("Post", back_populates="author", cascade="all, delete-orphan")[m
     comments = relationship("Comment", back_populates="author", cascade="all, delete-orphan")[m
[32m+[m[32m    replies = relationship("Reply", back_populates="author", cascade="all, delete-orphan")[m
     likes = relationship("Like", back_populates="user", cascade="all, delete-orphan")[m
 [m
 [m
[36m@@ -42,7 +51,7 @@[m [mclass Post(Base):[m
 [m
     author = relationship("User", back_populates="posts")[m
     comments = relationship("Comment", back_populates="post", cascade="all, delete-orphan")[m
[31m-    likes = relationship("Like", back_populates="post", cascade="all, delete-orphan")[m
[32m+[m[32m    likes = relationship("Like", foreign_keys="Like.post_id", back_populates="post", cascade="all, delete-orphan")[m
 [m
 [m
 class Comment(Base):[m
[36m@@ -63,21 +72,59 @@[m [mclass Comment(Base):[m
 [m
     post = relationship("Post", back_populates="comments")[m
     author = relationship("User", back_populates="comments")[m
[32m+[m[32m    replies = relationship("Reply", back_populates="comment", cascade="all, delete-orphan")[m
[32m+[m[32m    likes = relationship("Like", foreign_keys="Like.comment_id", back_populates="comment", cascade="all, delete-orphan")[m
[32m+[m
[32m+[m
[32m+[m[32mclass Reply(Base):[m
[32m+[m[32m    """[m
[32m+[m[32m    回复模型（评论的回复）[m
[32m+[m[32m    支持楼中楼，可以回复评论，也可以回复其他回复[m
[32m+[m[32m    """[m
[32m+[m[32m    __tablename__ = "replies"[m
[32m+[m
[32m+[m[32m    id = Column(Integer, primary_key=True, index=True)[m
[32m+[m[32m    # 母评论ID（必属）[m
[32m+[m[32m    comment_id = Column(Integer, ForeignKey("comments.id"), nullable=False)[m
[32m+[m[32m    # 父回复ID（可选，如果是回复回复则有值）[m
[32m+[m[32m    parent_reply_id = Column(Integer, ForeignKey("replies.id"), nullable=True)[m
[32m+[m[32m    author_id = Column(Integer, ForeignKey("users.id"), nullable=False)[m
[32m+[m[32m    content = Column(Text, nullable=False)[m
[32m+[m[32m    created_at = Column(DateTime, default=datetime.utcnow, nullable=False)[m
[32m+[m[41m    [m
[32m+[m[32m    # 热度相关字段[m
[32m+[m[32m    hot_score = Column(Integer, default=0, nullable=False, index=True)  # 热度分数[m
[32m+[m[32m    last_hot_update = Column(DateTime, default=datetime.utcnow, nullable=False)  # 上次热度更新时间[m
[32m+[m
[32m+[m[32m    comment = relationship("Comment", back_populates="replies")[m
[32m+[m[32m    author = relationship("User", back_populates="replies")[m
[32m+[m[32m    # 自引用关系，用于获取子回复[m
[32m+[m[32m    parent_reply = relationship("Reply", remote_side=[id], back_populates="child_replies")[m
[32m+[m[32m    child_replies = relationship("Reply", back_populates="parent_reply", cascade="all, delete-orphan")[m
[32m+[m[32m    likes = relationship("Like", foreign_keys="Like.reply_id", back_populates="reply", cascade="all, delete-orphan")[m
 [m
 [m
 class Like(Base):[m
     """[m
[31m-    点赞模型[m
[32m+[m[32m    点赞模型（通用）[m
[32m+[m[32m    支持帖子、评论、回复的点赞[m
     """[m
     __tablename__ = "likes"[m
 [m
     id = Column(Integer, primary_key=True, index=True)[m
     user_id = Column(Integer, ForeignKey("users.id"), nullable=False)[m
[31m-    post_id = Column(Integer, ForeignKey("posts.id"), nullable=False)[m
[32m+[m[41m    [m
[32m+[m[32m    # 点赞目标（三选一）[m
[32m+[m[32m    post_id = Column(Integer, ForeignKey("posts.id"), nullable=True)[m
[32m+[m[32m    comment_id = Column(Integer, ForeignKey("comments.id"), nullable=True)[m
[32m+[m[32m    reply_id = Column(Integer, ForeignKey("replies.id"), nullable=True)[m
[32m+[m[41m    [m
     created_at = Column(DateTime, default=datetime.utcnow, nullable=False)[m
 [m
     user = relationship("User", back_populates="likes")[m
[31m-    post = relationship("Post", back_populates="likes")[m
[32m+[m[32m    post = relationship("Post", foreign_keys=[post_id], back_populates="likes")[m
[32m+[m[32m    comment = relationship("Comment", foreign_keys=[comment_id], back_populates="likes")[m
[32m+[m[32m    reply = relationship("Reply", foreign_keys=[reply_id], back_populates="likes")[m
 [m
 [m
 class Follow(Base):[m
warning: in the working copy of 'social_platform/app/hot_score.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/social_platform/app/hot_score.py b/social_platform/app/hot_score.py[m
[1mindex e6a530b..fa17104 100644[m
[1m--- a/social_platform/app/hot_score.py[m
[1m+++ b/social_platform/app/hot_score.py[m
[36m@@ -1,9 +1,11 @@[m
 """[m
[31m-帖子热度计算模块[m
[31m-提供热度计算、衰减和更新功能[m
[32m+[m[32m热度计算模块[m
[32m+[m[32m提供帖子、评论、回复的热度计算、衰减和更新功能[m
 [m
[31m-热度公式: score = (点赞数*1 + 评论数*2) * 时间衰减系数[m
[31m-评论热度公式: score = 点赞数*1 * 时间衰减系数[m
[32m+[m[32m热度公式:[m
[32m+[m[32m- 帖子: score = (点赞数*1 + 评论数*2) * 时间衰减系数 + 新鲜度加成[m
[32m+[m[32m- 评论: score = (点赞数*1 + 回复数*2) * 时间衰减系数[m
[32m+[m[32m- 回复: score = (点赞数*1 + 子回复数*2) * 时间衰减系数[m
 """[m
 import math[m
 import random[m
[36m@@ -18,11 +20,12 @@[m [mfrom . import models[m
 # 热度计算配置[m
 HOT_SCORE_CONFIG = {[m
     "like_weight": 1,      # 点赞权重[m
[31m-    "comment_weight": 2,   # 评论权重[m
[31m-    "decay_half_life": 24, # 半衰期（小时）- 24小时后热度减半[m
[31m-    "freshness_bonus": 50, # 新鲜度加成（24小时内的新帖子）[m
[32m+[m[32m    "comment_weight": 2,   # 评论/回复权重[m
[32m+[m[32m    "decay_half_life": 24, # 帖子半衰期（小时）[m
[32m+[m[32m    "freshness_bonus": 50, # 新鲜度加成[m
     "freshness_window": 24, # 新鲜度窗口（小时）[m
[31m-    "comment_decay_half_life": 12, # 评论半衰期（小时）- 评论热度衰减更快[m
[32m+[m[32m    "comment_decay_half_life": 12, # 评论半衰期（小时）[m
[32m+[m[32m    "reply_decay_half_life": 8,    # 回复半衰期（小时）- 回复衰减更快[m
 }[m
 [m
 [m
[36m@@ -46,98 +49,103 @@[m [mdef calculate_hot_score(likes_count: int, comments_count: int,[m
     [m
     # 计算时间衰减[m
     now = datetime.utcnow()[m
[31m-    [m
[31m-    # 使用上次更新时间或创建时间作为参考[m
     reference_time = last_update if last_update else created_at[m
[31m-    [m
[31m-    # 计算经过的时间（小时）[m
     hours_passed = (now - reference_time).total_seconds() / 3600[m
     [m
[31m-    # 指数衰减: decay = 0.5^(t/half_life)[m
[31m-    # 这样半衰期后热度减半[m
[32m+[m[32m    # 指数衰减[m
     half_life = HOT_SCORE_CONFIG["decay_half_life"][m
     decay_factor = math.pow(0.5, hours_passed / half_life)[m
     [m
     # 应用衰减[m
     decayed_score = base_score * decay_factor[m
     [m
[31m-    # 新鲜度保护：24小时内的新帖子获得加成[m
[32m+[m[32m    # 新鲜度保护[m
     hours_since_created = (now - created_at).total_seconds() / 3600[m
     freshness_window = HOT_SCORE_CONFIG["freshness_window"][m
     [m
     if hours_since_created < freshness_window:[m
[31m-        # 新鲜度加成随时间线性减少[m
         freshness_factor = 1 - (hours_since_created / freshness_window)[m
         freshness_bonus = HOT_SCORE_CONFIG["freshness_bonus"] * freshness_factor[m
         decayed_score += freshness_bonus[m
     [m
[31m-    # 确保最小值为0[m
[31m-    final_score = max(0, int(decayed_score))[m
[31m-    [m
[31m-    return final_score[m
[32m+[m[32m    return max(0, int(decayed_score))[m
 [m
 [m
[31m-def calculate_comment_hot_score(likes_count: int, created_at: datetime,[m
[31m-                                last_update: Optional[datetime] = None) -> int:[m
[32m+[m[32mdef calculate_comment_hot_score(likes_count: int, replies_count: int,[m
[32m+[m[32m                                created_at: datetime, last_update: Optional[datetime] = None) -> int:[m
     """[m
     计算评论热度分数[m
     [m
[31m-    评论热度 = 点赞数 * 时间衰减系数[m
[31m-    评论半衰期更短（12小时），衰减更快[m
[32m+[m[32m    评论热度 = (点赞数*1 + 回复数*2) * 时间衰减系数[m
     [m
     Args:[m
         likes_count: 点赞数[m
[32m+[m[32m        replies_count: 回复数[m
         created_at: 评论创建时间[m
         last_update: 上次热度更新时间（可选）[m
         [m
     Returns:[m
         int: 热度分数[m
     """[m
[31m-    # 基础分数 = 点赞*1[m
[31m-    base_score = likes_count * HOT_SCORE_CONFIG["like_weight"][m
[32m+[m[32m    # 基础分数 = 点赞*1 + 回复*2[m
[32m+[m[32m    base_score = likes_count * HOT_SCORE_CONFIG["like_weight"] + \[m
[32m+[m[32m                 replies_count * HOT_SCORE_CONFIG["comment_weight"][m
     [m
     # 计算时间衰减[m
     now = datetime.utcnow()[m
[31m-    [m
[31m-    # 使用上次更新时间或创建时间作为参考[m
     reference_time = last_update if last_update else created_at[m
[31m-    [m
[31m-    # 计算经过的时间（小时）[m
     hours_passed = (now - reference_time).total_seconds() / 3600[m
     [m
[31m-    # 评论使用更短的半衰期（12小时）[m
[32m+[m[32m    # 评论使用更短的半衰期[m
     half_life = HOT_SCORE_CONFIG["comment_decay_half_life"][m
     decay_factor = math.pow(0.5, hours_passed / half_life)[m
     [m
[31m-    # 应用衰减[m
     decayed_score = base_score * decay_factor[m
[31m-    [m
[31m-    # 确保最小值为0[m
[31m-    final_score = max(0, int(decayed_score))[m
[31m-    [m
[31m-    return final_score[m
[32m+[m[32m    return max(0, int(decayed_score))[m
 [m
 [m
[31m-def update_post_hot_score(db: Session, post_id: int) -> int:[m
[32m+[m[32mdef calculate_reply_hot_score(likes_count: int, child_replies_count: int,[m
[32m+[m[32m                              created_at: datetime, last_update: Optional[datetime] = None) -> int:[m
     """[m
[31m-    更新单个帖子的热度分数[m
[32m+[m[32m    计算回复热度分数[m
[32m+[m[41m    [m
[32m+[m[32m    回复热度 = (点赞数*1 + 子回复数*2) * 时间衰减系数[m
     [m
     Args:[m
[31m-        db: 数据库会话[m
[31m-        post_id: 帖子ID[m
[32m+[m[32m        likes_count: 点赞数[m
[32m+[m[32m        child_replies_count: 子回复数[m
[32m+[m[32m        created_at: 回复创建时间[m
[32m+[m[32m        last_update: 上次热度更新时间（可选）[m
         [m
     Returns:[m
[31m-        int: 更新后的热度分数[m
[32m+[m[32m        int: 热度分数[m
     """[m
[32m+[m[32m    # 基础分数 = 点赞*1 + 子回复*2[m
[32m+[m[32m    base_score = likes_count * HOT_SCORE_CONFIG["like_weight"] + \[m
[32m+[m[32m                 child_replies_count * HOT_SCORE_CONFIG["comment_weight"][m
[32m+[m[41m    [m
[32m+[m[32m    # 计算时间衰减[m
[32m+[m[32m    now = datetime.utcnow()[m
[32m+[m[32m    reference_time = last_update if last_update else created_at[m
[32m+[m[32m    hours_passed = (now - reference_time).total_seconds() / 3600[m
[32m+[m[41m    [m
[32m+[m[32m    # 回复使用最短的半衰期[m
[32m+[m[32m    half_life = HOT_SCORE_CONFIG["reply_decay_half_life"][m
[32m+[m[32m    decay_factor = math.pow(0.5, hours_passed / half_life)[m
[32m+[m[41m    [m
[32m+[m[32m    decayed_score = base_score * decay_factor[m
[32m+[m[32m    return max(0, int(decayed_score))[m
[32m+[m
[32m+[m
[32m+[m[32mdef update_post_hot_score(db: Session, post_id: int) -> int:[m
[32m+[m[32m    """更新单个帖子的热度分数"""[m
     post = db.query(models.Post).filter(models.Post.id == post_id).first()[m
     if not post:[m
         return 0[m
     [m
[31m-    # 获取点赞数和评论数[m
     likes_count = db.query(models.Like).filter(models.Like.post_id == post_id).count()[m
     comments_count = db.query(models.Comment).filter(models.Comment.post_id == post_id).count()[m
     [m
[31m-    # 计算新热度[m
     new_score = calculate_hot_sco
                   SSUUMMMMAARRYY OOFF LLEESSSS CCOOMMMMAANNDDSS

      Commands marked with * may be preceded by a number, _N.
      Notes in parentheses indicate the behavior if _N is given.
      A key preceded by a caret indicates the Ctrl key; thus ^K is ctrl-K.

  h  H                 Display this help.
  q  :q  Q  :Q  ZZ     Exit.
 ---------------------------------------------------------------------------

                           MMOOVVIINNGG

  e  ^E  j  ^N  CR  *  Forward  one line   (or _N lines).
  y  ^Y  k  ^K  ^P  *  Backward one line   (or _N lines).
  ESC-j             *  Forward  one file line (or _N file lines).
  ESC-k             *  Backward one file line (or _N file lines).
  f  ^F  ^V  SPACE  *  Forward  one window (or _N lines).
  b  ^B  ESC-v      *  Backward one window (or _N lines).
  z                 *  Forward  one window (and set window to _N).
  w                 *  Backward one window (and set window to _N).
  ESC-SPACE         *  Forward  one window, but don't stop at end-of-file.
  ESC-b             *  Backward one window, but don't stop at beginning-of-file.
  d  ^D             *  Forward  one half-window (and set half-window to _N).
  u  ^U             *  Backward one half-window (and set half-window to _N).
  ESC-)  RightArrow *  Right one half screen width (or _N positions).
  ESC-(  LeftArrow  *  Left  one half screen width (or _N positions).
  ESC-}  ^RightArrow   Right to last column displayed.
  ESC-{  ^LeftArrow    Left  to first column.
  F                    Forward forever; like "tail -f".
  ESC-F                Like F but stop when search pattern is found.
  r  ^R  ^L            Repaint screen.
  R                    Repaint screen, discarding buffered input.
        ---------------------------------------------------
        Default "window" is the screen height.
        Default "half-window" is half of the screen height.
 ---------------------------------------------------------------------------

                          SSEEAARRCCHHIINNGG

  /_p_a_t_t_e_r_n          *  Search forward for (_N-th) matching line.
  ?_p_a_t_t_e_r_n          *  Search backward for (_N-th) matching line.
  n                 *  Repeat previous search (for _N-th occurrence).
  N                 *  Repeat previous search in reverse direction.
  ESC-n             *  Repeat previous search, spanning files.
  ESC-N             *  Repeat previous search, reverse dir. & spanning files.
  ^O^N  ^On         *  Search forward for (_N-th) OSC8 hyperlink.
  ^O^P  ^Op         *  Search backward for (_N-th) OSC8 hyperlink.
  ^O^L  ^Ol            Jump to the currently selected OSC8 hyperlink.
  ESC-u                Undo (toggle) search highlighting.
  ESC-U                Clear search highlighting.
  &_p_a_t_t_e_r_n          *  Display only matching lines.
        ---------------------------------------------------
		Search is case-sensitive unless changed with -i or -I.
        A search pattern may begin with one or more of:
        ^N or !  Search for NON-matching lines.
        ^E or *  Search multiple files (pass thru END OF FILE).
        ^F or @  Start search at FIRST file (for /) or last file (for ?).
        ^K       Highlight matches, but don't move (KEEP position).
        ^R       Don't use REGULAR EXPRESSIONS.
        ^S _n     Search for match in _n-th parenthesized subpattern.
        ^W       WRAP search if no match found.
        ^L       Enter next character literally into pattern.
 ---------------------------------------------------------------------------

                           JJUUMMPPIINNGG

  g  <  ESC-<       *  Go to first line in file (or line _N).
  G  >  ESC->       *  Go to last line in file (or line _N).
  p  %              *  Go to beginning of file (or _N percent into file).
  t                 *  Go to the (_N-th) next tag.
  T                 *  Go to the (_N-th) previous tag.
  {  (  [           *  Find close bracket } ) ].
  }  )  ]           *  Find open bracket { ( [.
  ESC-^F _<_c_1_> _<_c_2_>  *  Find close bracket _<_c_2_>.
  ESC-^B _<_c_1_> _<_c_2_>  *  Find open bracket _<_c_1_>.
        ---------------------------------------------------
        Each "find close bracket" command goes forward to the close bracket 
          matching the (_N-th) open bracket in the top line.
        Each "find open bracket" command goes backward to the open bracket 
          matching the (_N-th) close bracket in the bottom line.

  m_<_l_e_t_t_e_r_>            Mark the current top line with <letter>.
  M_<_l_e_t_t_e_r_>            Mark the current bottom line with <letter>.
  '_<_l_e_t_t_e_r_>            Go to a previously marked position.
  ''                   Go to the previous position.
  ^X^X                 Same as '.
  ESC-m_<_l_e_t_t_e_r_>        Clear a mark.
        ---------------------------------------------------
        A mark is any upper-case or lower-case letter.
        Certain marks are predefined:
             ^  means  beginning of the file
             $  means  end of the file
 ---------------------------------------------------------------------------

                        CCHHAANNGGIINNGG FFIILLEESS

  :e [_f_i_l_e]            Examine a new file.
  ^X^V                 Same as :e.
  :n                *  Examine the (_N-th) next file from the command line.
  :p                *  Examine the (_N-th) previous file from the command line.
  :x                *  Examine the first (or _N-th) file from the command line.
  ^O^O                 Open the currently selected OSC8 hyperlink.
  :d                   Delete the current file from the command line list.
  =  ^G  :f            Print current file name.
 ---------------------------------------------------------------------------

                    MMIISSCCEELLLLAANNEEOOUUSS CCOOMMMMAANNDDSS

  -_<_f_l_a_g_>              Toggle a command line option [see OPTIONS below].
  --_<_n_a_m_e_>             Toggle a command line option, by name.
  __<_f_l_a_g_>              Display the setting of a command line option.
  ___<_n_a_m_e_>             Display the setting of an option, by name.
  +_c_m_d                 Execute the less cmd each time a new file is examined.

  !_c_o_m_m_a_n_d             Execute the shell command with $SHELL.
  #_c_o_m_m_a_n_d             Execute the shell command, expanded like a prompt.
  |XX_c_o_m_m_a_n_d            Pipe file between current pos & mark XX to shell command.
  s _f_i_l_e               Save input to a file.
  v                    Edit the current file with $VISUAL or $EDITOR.
  V                    Print version number of "less".
 ---------------------------------------------------------------------------

                           OOPPTTIIOONNSS

        Most options may be changed either on the command line,
        or from within less by using the - or -- command.
        Options may be given in one of two forms: either a single
        character preceded by a -, or a name preceded by --.

  -?  ........  --help
                  Display help (from command line).
  -a  ........  --search-skip-screen
                  Search skips current screen.
  -A  ........  --SEARCH-SKIP-SCREEN
                  Search starts just after target line.
  -b [_N]  ....  --buffers=[_N]
                  Number of buffers.
  -B  ........  --auto-buffers
                  Don't automatically allocate buffers for pipes.
  -c  ........  --clear-screen
                  Repaint by clearing rather than scrolling.
  -d  ........  --dumb
                  Dumb terminal.
  -D xx_c_o_l_o_r  .  --color=xx_c_o_l_o_r
                  Set screen colors.
  -e  -E  ....  --quit-at-eof  --QUIT-AT-EOF
                  Quit at end of file.
  -f  ........  --force
                  Force open non-regular files.
  -F  ........  --quit-if-one-screen
                  Quit if entire file fits on first screen.
  -g  ........  --hilite-search
                  Highlight only last match for searches.
  -G  ........  --HILITE-SEARCH
                  Don't highlight any matches for searches.
  -h [_N]  ....  --max-back-scroll=[_N]
                  Backward scroll limit.
  -i  ........  --ignore-case
                  Ignore case in searches that do not contain uppercase.
  -I  ........  --IGNORE-CASE
                  Ignore case in all searches.
  -j [_N]  ....  --jump-target=[_N]
                  Screen position of target lines.
  -J  ........  --status-column
                  Display a status column at left edge of screen.
  -k _f_i_l_e  ...  --lesskey-file=_f_i_l_e
                  Use a compiled lesskey file.
  -K  ........  --quit-on-intr
                  Exit less in response to ctrl-C.
  -L  ........  --no-lessopen
                  Ignore the LESSOPEN environment variable.
  -m  -M  ....  --long-prompt  --LONG-PROMPT
                  Set prompt style.
  -n .........  --line-numbers
                  Suppress line numbers in prompts and messages.
  -N .........  --LINE-NUMBERS
                  Display line number at start of each line.
  -o [_f_i_l_e] ..  --log-file=[_f_i_l_e]
                  Copy to log file (standard input only).
  -O [_f_i_l_e] ..  --LOG-FILE=[_f_i_l_e]
                  Copy to log file (unconditionally overwrite).
  -p _p_a_t_t_e_r_n .  --pattern=[_p_a_t_t_e_r_n]
                  Start at pattern (from command line).
  -P [_p_r_o_m_p_t]   --prompt=[_p_r_o_m_p_t]
                  Define new prompt.
  -q  -Q  ....  --quiet  --QUIET  --silent --SILENT
                  Quiet the terminal bell.
  -r  -R  ....  --raw-control-chars  --RAW-CONTROL-CHARS
                  Output "raw" control characters.
  -s  ........  --squeeze-blank-lines
                  Squeeze multiple blank lines.
  -S  ........  --chop-long-lines
                  Chop (truncate) long lines rather than wrapping.
  -t _t_a_g  ....  --tag=[_t_a_g]
                  Find a tag.
  -T [_t_a_g_s_f_i_l_e] --tag-file=[_t_a_g_s_f_i_l_e]
                  Use an alternate tags file.
  -u  -U  ....  --underline-special  --UNDERLINE-SPECIAL
                  Change handling of backspaces, tabs and carriage returns.
  -V  ........  --version
                  Display the version number of "less".
  -w  ........  --hilite-unread
                  Highlight first new line after forward-screen.
  -W  ........  --HILITE-UNREAD
                  Highlight first new line after any forward movement.
  -x [_N[,...]]  --tabs=[_N[,...]]
                  Set tab stops.
  -X  ........  --no-init
                  Don't use termcap init/deinit strings.
  -y [_N]  ....  --max-forw-scroll=[_N]
                  Forward scroll limit.
  -z [_N]  ....  --window=[_N]
                  Set size of window.
  -" [_c[_c]]  .  --quotes=[_c[_c]]
                  Set shell quote characters.
  -~  ........  --tilde
                  Don't display tildes after end of file.
  -# [_N]  ....  --shift=[_N]
                  Set horizontal scroll amount (0 = one half screen width).

                --exit-follow-on-close
                  Exit F command on a pipe when writer closes pipe.
                --file-size
                  Automatically determine the size of the input file.
                --follow-name
                  The F command changes files if the input file is renamed.
                --form-feed
                  Stop scrolling when a form feed character is reached.
                --header=[_L[,_C[,_N]]]
                  Use _L lines (starting at line _N) and _C columns as headers.
                --incsearch
                  Search file as each pattern character is typed in.
                --intr=[_C]
                  Use _C instead of ^X to interrupt a read.
                --lesskey-context=_t_e_x_t
                  Use lesskey source file contents.
                --lesskey-src=_f_i_l_e
                  Use a lesskey source file.
                --line-num-width=[_N]
                  Set the width of the -N line number field to _N characters.
                --match-shift=[_N]
                  Show at least _N characters to the left of a search match.
                --modelines=[_N]
                  Read _N lines from the input file and look for vim modelines.
                --mouse
                  Enable mouse input.
                --no-edit-warn
                  Don't warn when using v command on a file opened via LESSOPEN.
                --no-keypad
                  Don't send termcap keypad init/deinit strings.
                --no-histdups
                  Remove duplicates from command history.
                --no-number-headers
                  Don't give line numbers to header lines.
                --no-paste
                  Ignore pasted input.
                --no-search-header-lines
                  Searches do not include header lines.
                --no-search-header-columns
                  Searches do not include header columns.
                --no-search-headers
                  Searches do not include header lines or columns.
                --no-vbell
                  Disable the terminal's visual bell.
                --redraw-on-quit
                  Redraw final screen when quitting.
                --rscroll=[_C]
                  Set the character used to mark truncated lines.
                --save-marks
                  Retain marks across invocations of less.
                --search-options=[EFKNRW-]
                  Set default options for every search.
                --show-preproc-errors
                  Display a message if preprocessor exits with an error status.
                --proc-backspace
                  Process backspaces for bold/underline.
                --PROC-BACKSPACE
                  Treat backspaces as control characters.
                --proc-return
                  Delete carriage returns before newline.
                --PROC-RETURN
                  Treat carriage returns as control characters.
                --proc-tab
                  Expand tabs to spaces.
                --PROC-TAB
                  Treat tabs as control characters.
                --status-col-width=[_N]
                  Set the width of the -J status column to _N characters.
                --status-line
                  Highlight or color the entire line containing a mark.
                --use-backslash
                  Subsequent options use backslash as escape char.
                --use-color
                  Enables colored text.
                --wheel-lines=[_N]
                  Each click of the mouse wheel moves _N lines.
                --wordwrap
                  Wrap lines at spaces.


 ---------------------------------------------------------------------------

                          LLIINNEE EEDDIITTIINNGG

        These keys can be used to edit text being entered 
        on the "command line" at the bottom of the screen.

 RightArrow ..................... ESC-l ... Move cursor right one character.
 LeftArrow ...................... ESC-h ... Move cursor left one character.
 ctrl-RightArrow  ESC-RightArrow  ESC-w ... Move cursor right one word.
 ctrl-LeftArrow   ESC-LeftArrow   ESC-b ... Move cursor left one word.
 HOME ........................... ESC-0 ... Move cursor to start of line.
 END ............................ ESC-$ ... Move cursor to end of line.
 BACKSPACE ................................ Delete char to left of cursor.
 DELETE ......................... ESC-x ... Delete char under cursor.
 ctrl-BACKSPACE   ESC-BACKSPACE ........... Delete word to left of cursor.
 ctrl-DELETE .... ESC-DELETE .... ESC-X ... Delete word under cursor.
 ctrl-U ......... ESC (MS-DOS only) ....... Delete entire line.
 UpArrow ........................ ESC-k ... Retrieve previous command line.
 DownArrow ...................... ESC-j ... Retrieve next command line.
 TAB ...................................... Complete filename & cycle.
 SHIFT-TAB ...................... ESC-TAB   Complete filename & reverse cycle.
 ctrl-L ................................... Complete filename, list all.

                   SSUUMMMMAARRYY OOFF LLEESSSS CCOOMMMMAANNDDSS

      Commands marked with * may be preceded by a number, _N.
      Notes in parentheses indicate the behavior if _N is given.
      A key preceded by a caret indicates the Ctrl key; thus ^K is ctrl-K.

  h  H                 Display this help.
  q  :q  Q  :Q  ZZ     Exit.
 ---------------------------------------------------------------------------

                           MMOOVVIINNGG

  e  ^E  j  ^N  CR  *  Forward  one line   (or _N lines).
  y  ^Y  k  ^K  ^P  *  Backward one line   (or _N lines).
  ESC-j             *  Forward  one file line (or _N file lines).
  ESC-k             *  Backward one file line (or _N file lines).
  f  ^F  ^V  SPACE  *  Forward  one window (or _N lines).
  b  ^B  ESC-v      *  Backward one window (or _N lines).
  z                 *  Forward  one window (and set window to _N).
  w                 *  Backward one window (and set window to _N).
  ESC-SPACE         *  Forward  one window, but don't stop at end-of-file.
  ESC-b             *  Backward one window, but don't stop at beginning-of-file.
  d  ^D             *  Forward  one half-window (and set half-window to _N).
  u  ^U             *  Backward one half-window (and set half-window to _N).
  ESC-)  RightArrow *  Right one half screen width (or _N positions).
  ESC-(  LeftArrow  *  Left  one half screen width (or _N positions).
  ESC-}  ^RightArrow   Right to last column displayed.
  ESC-{  ^LeftArrow    Left  to first column.
  F                    Forward forever; like "tail -f".
  ESC-F                Like F but stop when search pattern is found.
  r  ^R  ^L            Repaint screen.
  R                    Repaint screen, discarding buffered input.
        ---------------------------------------------------
        Default "window" is the screen height.
        Default "half-window" is half of the screen height.
 ---------------------------------------------------------------------------

                          SSEEAARRCCHHIINNGG

  /_p_a_t_t_e_r_n          *  Search forward for (_N-th) matching line.
  ?_p_a_t_t_e_r_n          *  Search backward for (_N-th) matching line.
  n                 *  Repeat previous search (for _N-th occurrence).
  N                 *  Repeat previous search in reverse direction.
  ESC-n             *  Repeat previous search, spanning files.
  ESC-N             *  Repeat previous search, reverse dir. & spanning files.
  ^O^N  ^On         *  Search forward for (_N-th) OSC8 hyperlink.
  ^O^P  ^Op         *  Search backward for (_N-th) OSC8 hyperlink.
  ^O^L  ^Ol            Jump to the currently selected OSC8 hyperlink.
  ESC-u                Undo (toggle) search highlighting.
  ESC-U                Clear search highlighting.
  &_p_a_t_t_e_r_n          *  Display only matching lines.
        ---------------------------------------------------
		Search is case-sensitive unless changed with -i or -I.
        A search pattern may begin with one or more of:
        ^N or !  Search for NON-matching lines.
        ^E or *  Search multiple files (pass thru END OF FILE).
        ^F or @  Start search at FIRST file (for /) or last file (for ?).
        ^K       Highlight matches, but don't move (KEEP position).
        ^R       Don't use REGULAR EXPRESSIONS.
        ^S _n     Search for match in _n-th parenthesized subpattern.
        ^W       WRAP search if no match found.
        ^L       Enter next character literally into pattern.
 ---------------------------------------------------------------------------

                           JJUUMMPPIINNGG

  g  <  ESC-<       *  Go to first line in file (or line _N).
  G  >  ESC->       *  Go to last line in file (or line _N).
  p  %              *  Go to beginning of file (or _N percent into file).
  t                 *  Go to the (_N-th) next tag.
  T                 *  Go to the (_N-th) previous tag.
  {  (  [           *  Find close bracket } ) ].
  }  )  ]           *  Find open bracket { ( [.
  ESC-^F _<_c_1_> _<_c_2_>  *  Find close bracket _<_c_2_>.
  ESC-^B _<_c_1_> _<_c_2_>  *  Find open bracket _<_c_1_>.
        ---------------------------------------------------
        Each "find close bracket" command goes forward to the close bracket 
          matching the (_N-th) open bracket in the top line.
        Each "find open bracket" command goes backward to the open bracket 
          matching the (_N-th) close bracket in the bottom line.

  m_<_l_e_t_t_e_r_>            Mark the current top line with <letter>.
  M_<_l_e_t_t_e_r_>            Mark the current bottom line with <letter>.
  '_<_l_e_t_t_e_r_>            Go to a previously marked position.
  ''                   Go to the previous position.
  ^X^X                 Same as '.
  ESC-m_<_l_e_t_t_e_r_>        Clear a mark.
        ---------------------------------------------------
        A mark is any upper-case or lower-case letter.
        Certain marks are predefined:
             ^  means  beginning of the file
             $  means  end of the file
 ---------------------------------------------------------------------------

                        CCHHAANNGGIINNGG FFIILLEESS

  :e [_f_i_l_e]            Examine a new file.
  ^X^V                 Same as :e.
  :n                *  Examine the (_N-th) next file from the command line.
  :p                *  Examine the (_N-th) previous file from the command line.
  :x                *  Examine the first (or _N-th) file from the command line.
  ^O^O                 Open the currently selected OSC8 hyperlink.
  :d                   Delete the current file from the command line list.
  =  ^G  :f            Print current file name.
 ---------------------------------------------------------------------------

                    MMIISSCCEELLLLAANNEEOOUUSS CCOOMMMMAANNDDSS

  -_<_f_l_a_g_>              Toggle a command line option [see OPTIONS below].
  --_<_n_a_m_e_>             Toggle a command line option, by name.
  __<_f_l_a_g_>              Display the setting of a command line option.
  ___<_n_a_m_e_>             Display the setting of an option, by name.
  +_c_m_d                 Execute the less cmd each time a new file is examined.

  !_c_o_m_m_a_n_d             Execute the shell command with $SHELL.
  #_c_o_m_m_a_n_d             Execute the shell command, expanded like a prompt.
  |XX_c_o_m_m_a_n_d            Pipe file between current pos & mark XX to shell command.
  s _f_i_l_e               Save input to a file.
  v                    Edit the current file with $VISUAL or $EDITOR.
  V                    Print version number of "less".
 ---------------------------------------------------------------------------

                           OOPPTTIIOONNSS

        Most options may be changed either on the command line,
        or from within less by using the - or -- command.
        Options may be given in one of two forms: either a single
        character preceded by a -, or a name preceded by --.

  -?  ........  --help
                  Display help (from command line).
  -a  ........  --search-skip-screen
                  Search skips current screen.
  -A  ........  --SEARCH-SKIP-SCREEN
                  Search starts just after target line.
  -b [_N]  ....  --buffers=[_N]
                  Number of buffers.
  -B  ........  --auto-buffers
                  Don't automatically allocate buffers for pipes.
  -c  ........  --clear-screen
                  Repaint by clearing rather than scrolling.
  -d  ........  --dumb
                  Dumb terminal.
  -D xx_c_o_l_o_r  .  --color=xx_c_o_l_o_r
                  Set screen colors.
  -e  -E  ....  --quit-at-eof  --QUIT-AT-EOF
                  Quit at end of file.
  -f  ........  --force
                  Force open non-regular files.
  -F  ........  --quit-if-one-screen
                  Quit if entire file fits on first screen.
  -g  ........  --hilite-search
                  Highlight only last match for searches.
  -G  ........  --HILITE-SEARCH
                  Don't highlight any matches for searches.
  -h [_N]  ....  --max-back-scroll=[_N]
                  Backward scroll limit.
  -i  ........  --ignore-case
                  Ignore case in searches that do not contain uppercase.
  -I  ........  --IGNORE-CASE
                  Ignore case in all searches.
  -j [_N]  ....  --jump-target=[_N]
                  Screen position of target lines.
  -J  ........  --status-column
                  Display a status column at left edge of screen.
  -k _f_i_l_e  ...  --lesskey-file=_f_i_l_e
                  Use a compiled lesskey file.
  -K  ........  --quit-on-intr
                  Exit less in response to ctrl-C.
  -L  ........  --no-lessopen
                  Ignore the LESSOPEN environment variable.
  -m  -M  ....  --long-prompt  --LONG-PROMPT
                  Set prompt style.
  -n .........  --line-numbers
                  Suppress line numbers in prompts and messages.
  -N .........  --LINE-NUMBERS
                  Display line number at start of each line.
  -o [_f_i_l_e] ..  --log-file=[_f_i_l_e]
                  Copy to log file (standard input only).
  -O [_f_i_l_e] ..  --LOG-FILE=[_f_i_l_e]
                  Copy to log file (unconditionally overwrite).
  -p _p_a_t_t_e_r_n .  --pattern=[_p_a_t_t_e_r_n]
                  Start at pattern (from command line).
  -P [_p_r_o_m_p_t]   --prompt=[_p_r_o_m_p_t]
                  Define new prompt.
  -q  -Q  ....  --quiet  --QUIET  --silent --SILENT
                  Quiet the terminal bell.
  -r  -R  ....  --raw-control-chars  --RAW-CONTROL-CHARS
                  Output "raw" control characters.
  -s  ........  --squeeze-blank-lines
                  Squeeze multiple blank lines.
  -S  ........  --chop-long-lines
                  Chop (truncate) long lines rather than wrapping.
  -t _t_a_g  ....  --tag=[_t_a_g]
                  Find a tag.
  -T [_t_a_g_s_f_i_l_e] --tag-file=[_t_a_g_s_f_i_l_e]
                  Use an alternate tags file.
  -u  -U  ....  --underline-special  --UNDERLINE-SPECIAL
                  Change handling of backspaces, tabs and carriage returns.
  -V  ........  --version
                  Display the version number of "less".
  -w  ........  --hilite-unread
                  Highlight first new line after forward-screen.
  -W  ........  --HILITE-UNREAD
                  Highlight first new line after any forward movement.
  -x [_N[,...]]  --tabs=[_N[,...]]
                  Set tab stops.
  -X  ........  --no-init
                  Don't use termcap init/deinit strings.
  -y [_N]  ....  --max-forw-scroll=[_N]
                  Forward scroll limit.
  -z [_N]  ....  --window=[_N]
                  Set size of window.
  -" [_c[_c]]  .  --quotes=[_c[_c]]
                  Set shell quote characters.
  -~  ........  --tilde
                  Don't display tildes after end of file.
  -# [_N]  ....  --shift=[_N]
                  Set horizontal scroll amount (0 = one half screen width).

                --exit-follow-on-close
                  Exit F command on a pipe when writer closes pipe.
                --file-size
                  Automatically determine the size of the input file.
                --follow-name
                  The F command changes files if the input file is renamed.
                --form-feed
                  Stop scrolling when a form feed character is reached.
                --header=[_L[,_C[,_N]]]
                  Use _L lines (starting at line _N) and _C columns as headers.
                --incsearch
                  Search file as each pattern character is typed in.
                --intr=[_C]
                  Use _C instead of ^X to interrupt a read.
                --lesskey-context=_t_e_x_t
                  Use lesskey source file contents.
                --lesskey-src=_f_i_l_e
                  Use a lesskey source file.
                --line-num-width=[_N]
                  Set the width of the -N line number field to _N characters.
                --match-shift=[_N]
                  Show at least _N characters to the left of a search match.
                --modelines=[_N]
                  Read _N lines from the input file and look for vim modelines.
                --mouse
                  Enable mouse input.
                --no-edit-warn
                  Don't warn when using v command on a file opened via LESSOPEN.
                --no-keypad
                  Don't send termcap keypad init/deinit strings.
                --no-histdups
                  Remove duplicates from command history.
                --no-number-headers
                  Don't give line numbers to header lines.
                --no-paste
                  Ignore pasted input.
                --no-search-header-lines
                  Searches do not include header lines.
                --no-search-header-columns
                  Searches do not include header columns.
                --no-search-headers
                  Searches do not include header lines or columns.
                --no-vbell
                  Disable the terminal's visual bell.
                --redraw-on-quit
                  Redraw final screen when quitting.
                --rscroll=[_C]
                  Set the character used to mark truncated lines.
                --save-marks
                  Retain marks across invocations of less.
                --search-options=[EFKNRW-]
                  Set default options for every search.
                --show-preproc-errors
                  Display a message if preprocessor exits with an error status.
                --proc-backspace
                  Process backspaces for bold/underline.
                --PROC-BACKSPACE
                  Treat backspaces as control characters.
                --proc-return
                  Delete carriage returns before newline.
                --PROC-RETURN
                  Treat carriage returns as control characters.
                --proc-tab
                  Expand tabs to spaces.
                --PROC-TAB
                  Treat tabs as control characters.
                --status-col-width=[_N]
                  Set the width of the -J status column to _N characters.
                --status-line
                  Highlight or color the entire line containing a mark.
                --use-backslash
                  Subsequent options use backslash as escape char.
                --use-color
                  Enables colored text.
                --wheel-lines=[_N]
                  Each click of the mouse wheel moves _N lines.
                --wordwrap
                  Wrap lines at spaces.


 ---------------------------------------------------------------------------

                          LLIINNEE EEDDIITTIINNGG

        These keys can be used to edit text being entered 
        on the "command line" at the bottom of the screen.

 RightArrow ..................... ESC-l ... Move cursor right one character.
 LeftArrow ...................... ESC-h ... Move cursor left one character.
 ctrl-RightArrow  ESC-RightArrow  ESC-w ... Move cursor right one word.
 ctrl-LeftArrow   ESC-LeftArrow   ESC-b ... Move cursor left one word.
 HOME ........................... ESC-0 ... Move cursor to start of line.
 END ............................ ESC-$ ... Move cursor to end of line.
 BACKSPACE ................................ Delete char to left of cursor.
 DELETE ......................... ESC-x ... Delete char under cursor.
 ctrl-BACKSPACE   ESC-BACKSPACE ........... Delete word to left of cursor.
 ctrl-DELETE .... ESC-DELETE .... ESC-X ... Delete word under cursor.
 ctrl-U ......... ESC (MS-DOS only) ....... Delete entire line.
 UpArrow ........................ ESC-k ... Retrieve previous command line.
 DownArrow ...................... ESC-j ... Retrieve next command line.
 TAB ...................................... Complete filename & cycle.
 SHIFT-TAB ...................... ESC-TAB   Complete filename & reverse cycle.
 ctrl-L ................................... Complete filename, list all.
